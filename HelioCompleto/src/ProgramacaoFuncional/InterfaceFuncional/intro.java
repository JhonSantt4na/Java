package ProgramacaoFuncional.InterfaceFuncional;

public class intro {
	// Interface funcional
	//É uma interface que possui um único metodo abstrato.
	// Suas implementações serão tratadas como expressões lambda.
	
	//ex: Criamos uma Classe com 1 unico metodo para ser usado o seu obj como argumento de uma função
	//public class MyComparator implements Comparator<Product> {
	//			@Override
	//			public int compare(Product p1, Product p2) {
	//				return p1.getName().toUpperCase().compareTo(p2.getName().toUpperCase());
	//			}
	//	}
	
	//public static void main(String[] args) {
	//		(...)
	//		list.sort(new MyComparator());
	
	// Em linguagem funcional pura isso não existe.
	// Algumas interfaces Funcionais comuns
	// Predicate
	//Function
	//Consumer :
	//Nota: ao contrário das outras interfaces funcionais, no caso do Consumer, é
	//esperado ele possa gerar efeitos colaterais. pois ela retorna void ou seja pode mudar qualquer coisa
	
	
	
}
